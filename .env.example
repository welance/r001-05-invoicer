CLOCKIFY_API_KEY=your_clockify_api_key
CLOCKIFY_WORKSPACE_ID=your_workspace_id
QONTO_LOGIN=your-org-slug
QONTO_SECRET_KEY=your_qonto_secret_key

# Optional: only needed for LLM-assisted steps (client extraction, error explanation)
ANTHROPIC_API_KEY=your_anthropic_api_key

# Gmail API (OAuth2) for `invoicer mail-draft` — also needs credentials.json + token.json
# in the repo root (both gitignored). See README for Google Cloud Console setup.
GMAIL_SENDER=you@yourdomain.com
# Display name used in the email signature. If omitted, derived from GMAIL_SENDER.
GMAIL_SENDER_NAME=Your Name
